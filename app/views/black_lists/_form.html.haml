= entry_form(buttons_bottom: false) do |f|
  = f.labeled_input_fields(:first_name, :last_name)
  = f.labeled_input_field(:pbs_number, help: t('black_lists.form.pbs_number_format'))
  = f.labeled_input_fields(:email, :phone_number, :reference_name, :reference_phone_number)
