- # encoding: utf-8
- #  Copyright (c) 2012-2015, Pfadibewegung Schweiz. This file is part of
- #  hitobito_pbs and licensed under the Affero General Public License version 3
- #  or later. See the COPYING file at the top-level directory or at
- #  https://github.com/hitobito/hitobito_pbs.


.modal.hide.fade{id: "#{action}_application_dialog"}
  .modal-content
    = standard_form(@application.next_open_approval,
                    url: send("#{action}_group_event_application_path", @group, @event, @application),
                    method: action == :approve ? :put : :delete,
                    stacked: true) do |f|
      .modal-header
        %button.close{"data-dismiss" => "modal", :type => "button"} ×
        %h4.modal-title #{Event::Participation.model_name.human} #{t("event.participations.actions_show.#{action}_button").downcase}

      .modal-body
        = f.labeled_input_field(:comment, class: 'span5')
        = save_form_buttons(f, nil, group_event_participation_path(@group, @event, @participation))
