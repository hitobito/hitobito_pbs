-#  Copyright (c) 2012-2015, Pfadibewegung Schweiz. This file is part of
-#  hitobito_pbs and licensed under the Affero General Public License version 3
-#  or later. See the COPYING file at the top-level directory or at
-#  https://github.com/hitobito/hitobito_pbs.

%dl.dl-horizontal

  -# TODO only show attr/values if value present

  - entry.used?(:abteilungsleitung_id) do
    = labeled_person_attr(entry, :abteilungsleitung)

  - entry.used?(:advisor_mountain_security_id) do
    = labeled_person_attr(entry, :advisor_mountain_security)

  - entry.used?(:advisor_snow_security_id) do
    = labeled_person_attr(entry, :advisor_snow_security)

  - entry.used?(:advisor_water_security_id) do
    = labeled_person_attr(entry, :advisor_water_security)

  - entry.used?(:coach_id) do
    = labeled_person_attr(entry, :coach)

  - entry.used?(:coach_confirmed) do
    = labeled_attr(entry, :coach_confirmed)
