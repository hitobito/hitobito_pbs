-#  Copyright (c) 2012-2015, Pfadibewegung Schweiz. This file is part of
-#  hitobito_pbs and licensed under the Affero General Public License version 3
-#  or later. See the COPYING file at the top-level directory or at
-#  https://github.com/hitobito/hitobito_pbs.

- entry.used?(:requires_approval_abteilung) do
  = f.labeled(:requires_approval) do
    - %w(requires_approval_abteilung requires_approval_region requires_approval_kantonalverband requires_approval_bund).each do |approval|
      = f.boolean_field(approval.to_sym, {caption: t(".#{approval}")})

- entry.used?(:paper_application_required) do
  = f.labeled_boolean_field(:paper_application_required, {caption: t('.paper_application_required_caption')})

- entry.used?(:participants_can_apply) do
  = f.labeled_boolean_field(:participants_can_apply, {caption: t('.participants_can_apply_caption')})

- entry.used?(:participants_can_cancel) do
  = f.labeled_boolean_field(:participants_can_cancel, {caption: t('.participants_can_cancel_caption')})
